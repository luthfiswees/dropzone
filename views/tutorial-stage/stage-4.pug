doctype html
html
  head
    //- import interactjs and bootstrap
    link(rel='stylesheet', href='/stylesheets/bootstrap/css/bootstrap.css')
    script(src="//ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js")
    script(src="/javascripts/interact.min.js")

    //- import game script and css
    link(rel="stylesheet", href="/stylesheets/stage-4/drag.css")
    link(rel="stylesheet", href="/stylesheets/dropzone.css")
    link(rel="stylesheet", href="/stylesheets/modal.css")
    script(src="/javascripts/stage-4/record.js")
    script(src="/javascripts/drag.js")
    script(src="/javascripts/stage-4/dropzone.js")
    script(src="/javascripts/tutorial-time.js")
    script(src="/javascripts/stage-4/instruction.js")
    script(src="/javascripts/tutorial.js")

  body
    include stage-navbar.pug
    
    //- Pop up
    div(id="ilm-modal-box-introduction-1", class="ilm-modal-show")
      div(class="modal-dialog")
        div(class="modal-content")
          div(class="modal-header")
            div(class="modal-title")
              h4 Tutorial
          div(class="modal-body")
            div(class="ilm-modal-content")
              h4 Introducing the (¬) NOT Operator
              br
              p This operator is also one of the fundamental operators in <b>Boolean Logic</b>
          div(class="modal-footer")
            button(id="close-modal-box-introduction-1", type="button", class="btn btn-primary") Next
            
    div(id="ilm-modal-box-introduction-2", class="ilm-modal")
      div(class="modal-dialog")
        div(class="modal-content")
          div(class="modal-header")
            div(class="modal-title")
              h4 Tutorial
            div(class="modal-body")
              div(class="ilm-modal-content")
                h4 NOT means that you need to satisfy other conditions other than the stated one.
                br
                p Look at this instructions (Sssst! This one --> <b>'¬BLUE'</b>)
                p This means, in order to complete the level you need to put the <b>GREEN</b> box inside the <b>Dropzone</b> 
                p (Since the only box other than <b>BLUE</b> box is <b>GREEN</b> box)
            div(class="modal-footer")
              button(id="close-modal-box-introduction-2", type="button", class="btn btn-primary") Next
              
    div(id="ilm-modal-box-introduction-3", class="ilm-modal")
      div(class="modal-dialog")
        div(class="modal-content")
          div(class="modal-header")
            div(class="modal-title")
              h4 Tutorial
            div(class="modal-body")
              div(class="ilm-modal-content")
                h4 So let's get started!
                br
                p Put the <b>GREEN</b> box inside the <b>Dropzone</b> to complete the level.
              div(class="modal-footer")
                button(id="close-modal-box-introduction-3", type="button", class="btn btn-primary") Next
    
    //- Main content
    article
      div(id="box-1", class="box-draggable")
        p=text

      div(id="box-2", class="box-draggable")
        p=text

      div(id="box-dropzone", class="dropzone")
        div(class="dropzone-text")
          p(id="dropzone-title") Dropzone
          p(id="dropzone-instruction")
            b ¬BLUE

      div(id="ilm-modal-box", class="ilm-modal")
        div(class="modal-dialog")
          div(class="modal-content")
            div(class="modal-header")
              div(class="modal-title")
                h4 Level Complete
            div(class="modal-body")
              div(class="ilm-modal-content")
                h3 Congratulations on completing the task!
            div(class="modal-footer")
              button(type="button", class="btn btn-primary")
                a(id="ilm-button-text-color", href="/stage-5") Next Task

      div(id="ilm-modal-box-instruction", class="ilm-modal")
        div(class="modal-dialog")
          div(class="modal-content")
            div(class="modal-header")
              div(class="modal-title")
                h4 Instructions
            div(class="modal-body")
              div(class="ilm-modal-content")
                h4 The <b>NOT</b> (¬) operator means that you need to satisfy other conditions other than the stated one.
                br
                p In this case, you should put any box as long as it's not a <b>blue</b> box inside the <b>Dropzone</b>.
            div(class="modal-footer")
              button(id="close-modal-box-insruction", type="button", class="btn btn-primary") Close Instruction

      include stage-lose-popup.pug
